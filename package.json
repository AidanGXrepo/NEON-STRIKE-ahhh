{
  "name": "neon-strike-lan",
  "version": "1.0.0",
  "description": "Neon Strike — LAN multiplayer FPS. Hosts the game files and the authoritative match server.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
